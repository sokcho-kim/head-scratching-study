# Code

```
var maxProfit = function(prices) {
    let total = 0;
    const len = prices.length;

    for(let i = 1;i<len; i++){
        const profit = Math.max(prices[i] - prices[i - 1], 0 )
        total+= profit;
    }
    return total
};

```
